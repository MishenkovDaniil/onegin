#include <assert.h>
#include <stdio.h>

#include "consts.h"
#include "read_onegin.h"
/*
int main ()
{
    struct Str_pointers pointers[4];

//    pointers[0].p = "foo";
  //  pointers[1].p = "poo";
    //pointers[2].p = "cook";

    char arr[100] = "ksfok\nffdfdf\ngdgdd\nfkkd";
    char *pstr = arr;;
    read_by_str (pstr, pointers);

    for (int i = 0; i < 4; i++)
    {
        printf ("%s\t", pointers[i].p);
        printf ("%d\n", pointers[i].number);
        printf ("%d\n", pointers[i].size);
    }

}*/
